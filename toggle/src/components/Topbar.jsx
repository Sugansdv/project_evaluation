import React from "react";

const Topbar = () => (
  <nav className="navbar navbar-dark bg-dark px-4">
    <span className="navbar-brand mb-0 h1 ms-5">useToggle App</span>
  </nav>
);

export default Topbar;